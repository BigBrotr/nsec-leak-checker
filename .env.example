# Required: Nostr private key for the DVM (nsec or hex)
NOSTR_PRIVATE_KEY=nsec1...

# Optional: comma-separated relay URLs (defaults to damus, nos.lol, primal, mostr)
# RELAYS=wss://relay.damus.io,wss://nos.lol
